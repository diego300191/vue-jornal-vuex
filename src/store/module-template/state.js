/*
El state es reactivo, cuando se modifica el state, vamos a notificar a todos los componentes o a varios lugares donde se esta escuchando los cambios del nuestro state.

*/

// export default () => ({});
